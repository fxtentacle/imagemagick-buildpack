#!/usr/bin/env bash

set -e

BIN_DIR=$(dirname $0)
mkdir -p "$1/bin/"
cp "$BIN_DIR/$STACK/*" "$1/bin/"
im_version=$($1/bin/identify -version 2>&1 | tr '\n' ' ' )
echo "-----> imagemagick-buildpack: Installed to app/bin. ${im_version}"

exit 0
